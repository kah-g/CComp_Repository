#include <stdio.h>
#include <stdlib.h>

int main () {
  int nElem, elem, i;
  char texto;
  scanf("%d", &nElem);
  for (i=0; i < nElem; i++) {
    scanf("%d", &elem);
  }
  while ((scanf("%c", &texto)) == 1) {
    
  }

  return 0;
}
